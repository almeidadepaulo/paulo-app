-- Configura View
SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO

-- Apaga view
IF  EXISTS (SELECT * FROM sys.views WHERE object_id = OBJECT_ID(N'[dbo].[VW_BN411_AC]'))
DROP VIEW [dbo].[VW_BN411_AC]
GO

-- Tela Analitica de Captacao
-- Cria View
CREATE VIEW [dbo].[VW_BN411_AC]
WITH ENCRYPTION
AS
 SELECT 
  BN411.BN411_NR_GRUPO,
  BN411.BN411_NR_INST,
  BN411.BN411_DT_MOVTO,
  BN411.BN411_ST_APROV,
  BN411.BN411_NR_INSTFI,
  BN411.BN411_VL_MOVTO
  
  FROM BN411 AS BN411

GO


